# Railway build configuration for static Next.js export
[providers]
nixpacks = { version = "v0.21.0" }

[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.build]
cmds = ["npm ci", "npm run build"]

[start]
cmd = "npm start"

